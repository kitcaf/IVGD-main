# 概述

目前的做法是：两个任务共同完成源定位的任务

第一个任务（粗粒度筛选源点）
基于本质上是基于独立传播模型，基于传播动力学模型（独立级联模型 IC）和GNN实现逆向推断的

第二个任务：在第一个任务获得的结果进行优化

## 想法

### “逆向时间”图序列模型

> GAT + RNN（纯逆向或者双向）

通过快照序列累计模型不断积累“谁是源点”的证据

### 第二个任务的约束

设计全新的、更复杂的拓扑约束。包括先验估计、密度约束
我甚至认为聚类的方式其实都是可以考虑的
